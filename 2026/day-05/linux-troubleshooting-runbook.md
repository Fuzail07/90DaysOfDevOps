Today I completed 2 days tasks due to some issue.

these are the commands I practiced today.

1. uname -a - it prints the system informations like kernel, system informations, etc.
2. lsb_release - a works same as uname.

3. mkdir - make a directory in the tmp dictionary and used cp command to copy the hosts name from etc.
4. Ps, Htop,vmstat, free - commands to monitor the cpu health and running process.

5. df, du, iostat, vmstat - commands to check the stress on the disk.

6. netstat, ss and ping - commands to check the network statistics.

7. checked the logs of the system in the /var/log/syslog.